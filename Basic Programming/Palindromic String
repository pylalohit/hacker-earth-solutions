#include<iostream>
using namespace std;
int main()
{
    string str;
    cin>>str;
    int l = 0,h=str.length()-1,flag=0;
    while(h>l)
    {
        if(str[l++] != str[h--])
        {
            flag = 1;
            break;
        }
    }
    flag == 0 ? cout<<"YES" : cout<<"NO";
    
}
