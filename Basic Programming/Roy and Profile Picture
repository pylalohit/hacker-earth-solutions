#include<iostream>
using namespace std;
int main()
{
	int l,n,w,h;
	cin>>l>>n;
	while(n--)
	{
		cin>>w>>h;
		if(w < l || h < l)
		{
			cout << "UPLOAD ANOTHER \n";
		}
		else if(w == h && (w>=l && h>=l) )
		{
			cout<<"ACCEPTED \n";
		}
		else
		{
			cout<<"CROP IT \n";

		}
	}
	
}
